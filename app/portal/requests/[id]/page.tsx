import{notFound}from"next/navigation";import{requirePortalUser}from"../../../lib/portal-auth";import{DataError,EmptyState,PortalLink,PortalPageHeader,Row,Status,text}from"../../_components";import{CommentForm,RequestAdminForm}from"../RequestForms";
export default async function RequestPage({params}:{params:Promise<{id:string}>}){const{id}=await params;const{supabase,profile}=await requirePortalUser();const comments=profile.role==="admin"?supabase.from("change_request_comments").select("*,author:profiles(id,full_name,role)").eq("request_id",id).order("created_at"):supabase.from("change_request_comments").select("*,author:profiles(id,full_name,role)").eq("request_id",id).eq("is_internal",false).order("created_at");const[requestResult,commentsResult,attachmentsResult]=await Promise.all([supabase.from("change_requests").select("*").eq("id",id).single(),comments,supabase.from("request_attachments").select("*").eq("request_id",id).order("created_at")]);if(!requestResult.data)notFound();const request=requestResult.data as Row;const errors=[commentsResult.error,attachmentsResult.error].filter(Boolean);return <div className="mx-auto max-w-4xl px-6 py-16"><Status value={request.status}/><PortalPageHeader eyebrow="Change request" title={text(request,"title")} description={text(request,"description")}/>{profile.role==="admin"&&<RequestAdminForm request={{id:String(request.id),status:String(request.status),admin_notes:typeof request.admin_notes==="string"?request.admin_notes:null}}/>}{errors.length>0&&<div className="mt-8"><DataError message="Some request details could not be loaded."/></div>}<section className="mt-14"><h2 className="text-3xl font-semibold">Conversation</h2><div className="mt-5">{commentsResult.data?.length?((commentsResult.data as Row[]).map((comment,index)=><article key={String(comment.id??index)} className="border-b border-[#E7DCC1]/10 py-5 text-sm leading-7 text-[#E7DCC1]/65">{comment.is_internal===true&&<span className="mr-3 text-[10px] uppercase text-[#C6A972]">Internal</span>}{text(comment,"body")}</article>)):<EmptyState>No comments yet.</EmptyState>}</div><CommentForm requestId={id} admin={profile.role==="admin"}/></section><section className="mt-14"><h2 className="text-3xl font-semibold">Attachments</h2><div className="mt-5">{attachmentsResult.data?.length?(attachmentsResult.data as Row[]).map((item,index)=><div key={String(item.id??index)} className="flex items-center justify-between gap-4 border-b border-[#E7DCC1]/10 py-4 text-sm"><span>{text(item,"file_name")}</span><PortalLink href={`/portal/request-attachments/${item.id}`}>Download</PortalLink></div>):<EmptyState>No attachments have been shared.</EmptyState>}</div></section></div>}
