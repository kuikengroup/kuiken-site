"use client";
import {useActionState} from "react";
import {createProject,inviteClient,type AdminState} from "./actions";
const field="rounded-xl bg-[#382F24] p-3";
export default function AdminForms({clients}:{clients:{id:string;name:string}[]}){const [inviteState,inviteAction,invitePending]=useActionState(inviteClient,{} as AdminState);const [projectState,projectAction,projectPending]=useActionState(createProject,{} as AdminState);return <div className="grid gap-6 lg:grid-cols-2"><form action={inviteAction} className="grid gap-4 rounded-[2rem] border border-[#E7DCC1]/10 bg-[#211C16] p-7"><h2 className="text-2xl font-semibold">Invite client user</h2><input name="fullName" required placeholder="Full name" className={field}/><input name="email" type="email" required placeholder="Email" className={field}/><ClientSelect clients={clients}/><Notice state={inviteState}/><button disabled={invitePending} className="rounded-full bg-[#C6A972] px-5 py-3 text-xs font-semibold uppercase tracking-[.18em] text-[#17130E]">{invitePending?"Sending…":"Send invitation"}</button></form><form action={projectAction} className="grid gap-4 rounded-[2rem] border border-[#E7DCC1]/10 bg-[#211C16] p-7"><h2 className="text-2xl font-semibold">Create project</h2><input name="name" required placeholder="Project name" className={field}/><textarea name="description" placeholder="Overview" className={field}/><ClientSelect clients={clients}/><Notice state={projectState}/><button disabled={projectPending} className="rounded-full bg-[#C6A972] px-5 py-3 text-xs font-semibold uppercase tracking-[.18em] text-[#17130E]">{projectPending?"Saving…":"Create project"}</button></form></div>}
function ClientSelect({clients}:{clients:{id:string;name:string}[]}){return <select name="clientId" required className={field}><option value="">Choose client</option>{clients.map(client=><option key={client.id} value={client.id}>{client.name}</option>)}</select>}
function Notice({state}:{state:AdminState}){return <>{state.error&&<p className="text-sm text-red-200">{state.error}</p>}{state.message&&<p className="text-sm text-[#C6A972]">{state.message}</p>}</>}
